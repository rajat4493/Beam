{
  "name": "live-support-layer",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --env-file=.env --experimental-strip-types apps/web/src/server.ts",
    "test": "node --experimental-strip-types --test tests/**/*.test.ts",
    "demo": "node --experimental-strip-types apps/web/src/server.ts",
    "db:migrate": "node --env-file=.env --experimental-strip-types scripts/migrate.ts",
    "validate:recovery": "node --env-file=.env --experimental-strip-types scripts/validate-recovery.ts"
  },
  "dependencies": {
    "pg": "^8.23.0"
  }
}
